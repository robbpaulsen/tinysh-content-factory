# ========================================
# API KEYS (Global - Required)
# ========================================

# Google Gemini API (for LLM script generation)
GOOGLE_API_KEY=your-google-gemini-api-key-here

# Together.ai API (for FLUX image generation)
TOGETHER_API_KEY=your-together-ai-api-key-here

# ========================================
# MEDIA SERVER (Global - Required)
# ========================================

# Local Media Server URL
MEDIA_SERVER_URL=http://localhost:8000
# Or use network IP if accessing from other machines: http://192.168.68.60:8000

# ========================================
# GOOGLE SHEETS (Global - Shared)
# ========================================

# Google Sheets Spreadsheet ID (same spreadsheet for all channels)
# Each channel uses a different tab/sheet (configured in channel.yaml)
GOOGLE_SHEET_ID=12roXp7Ds8S0nA_Us2MjdKkAldJYUdTV80A7vcjwR6yI

# ========================================
# GOOGLE OAUTH (Legacy Fallback - IGNORE for multi-channel)
# ========================================

# OAuth credentials path (ONLY used if NOT using --channel flag)
# For multi-channel: Place credentials.json in each channel directory instead:
#   channels/momentum_mindset/credentials.json
#   channels/wealth_wisdom/credentials.json
GOOGLE_CREDENTIALS_PATH=.credentials/credentials.json

# ========================================
# IMAGE GENERATION (Global)
# ========================================

# FLUX Model
FLUX_MODEL=black-forest-labs/FLUX.1-schnell-Free

# Image dimensions (can be overridden in channel.yaml)
IMAGE_WIDTH=768
IMAGE_HEIGHT=1344

# ========================================
# YOUTUBE (Global Defaults)
# ========================================

# Default privacy status for uploads (can be overridden in channel.yaml)
YOUTUBE_PRIVACY_STATUS=private

# Note: selfDeclaredMadeForKids=false by default
# Note: containsSyntheticMedia is NOT set (false) - only use if imitating real people
#       Setting it to true severely limits reach. Generic AI content does not require it.

# ========================================
# FFMPEG OPTIMIZATION (Global)
# ========================================

# Video Encoder
FFMPEG_ENCODER=auto              # Options: auto (detect), nvenc (GPU), x264 (CPU)
FFMPEG_PRESET=p4                 # NVENC: p1-p7 (p4=balanced) | x264: medium
FFMPEG_CQ=23                     # Quality: 18=best, 28=worst (default: 23)
FFMPEG_BITRATE=5M                # Target bitrate (5M recommended for Shorts)
FFMPEG_AUDIO_BITRATE=128k        # Audio bitrate (128k sufficient for Shorts)

# ========================================
# SEO OPTIMIZATION (Global)
# ========================================

# Enable/disable automatic SEO metadata generation with Gemini
SEO_ENABLED=true                 # Options: true, false (default: true)

# ========================================
# LOGGING (Global)
# ========================================

# Enable logging to file with rotation
LOG_TO_FILE=true                 # Options: true, false (default: true)

# Directory for log files
LOG_DIR=output/logs              # Default: output/logs

# Maximum age of log files to keep (in days)
LOG_MAX_AGE_DAYS=7               # Range: 1-30 (default: 7)

# ========================================
# CHANNEL-SPECIFIC CONFIGURATION
# ========================================
#
# The following settings are NOW configured per-channel in:
#   channels/<channel_name>/channel.yaml
#
# DO NOT add them to .env - they will be IGNORED:
#   - SUBREDDIT           → channel.yaml: content.subreddit
#   - CONTENT_TYPE        → channel.yaml: content.content_type
#   - ART_STYLE           → channel.yaml: image.style
#   - SHEET_NAME          → channel.yaml: content.sheet_tab
#   - ACTIVE_PROFILE      → channel.yaml: default_profile
#   - PROFILES_PATH       → channel.yaml: profiles_path
#   - YOUTUBE_CATEGORY_ID → channel.yaml: youtube.category_id
#   - YOUTUBE_TIMEZONE    → channel.yaml: youtube.schedule.timezone
#   - YOUTUBE_SCHEDULE_*  → channel.yaml: youtube.schedule.*
#
# See channels/momentum_mindset/channel.yaml for example configuration.
#
